kind: pipeline
type: docker
name: run_apps

steps:
  - name: Run Backend
    image: docker
    commands:
      - cd Watch2Gether_Backend
      - docker-compose up -d

  - name: Run Frontend
    image: docker
    commands:
      - cd watch2gether_client
      - docker-compose up -d
