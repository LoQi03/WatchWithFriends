kind: pipeline
type: docker
name: run_apps

steps:
  - name: Run Backend
    image: docker:dind
    commands:
      - cd Watch2Gether_Backend
      - sleep 10
      - docker-compose up -d

  - name: Run Frontend
    image: docker:dind
    commands:
      - cd watch2gether_client
      - sleep 10
      - docker-compose up -d
